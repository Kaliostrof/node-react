<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0"
    />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <title><%= title %></title>
  </head>

  <body>
    <%- include('./partials/user-header') %>

    <section class="h-80 w-80">
      <div class="container py-5 h-80">
        <% if (error) {%>
        <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <h1 class="mb-5">Заявки с формы</h1>

        <% if (appointments.length) { %>
        <table class="table">
          <thead>
            <tr>
              <th scope="col">Дата отправки</th>
              <th scope="col">ФИО</th>
              <th scope="col">Телефон</th>
              <th scope="col">Проблема</th>
            </tr>
          </thead>

          <% for(let i = 0; i < appointments.length; i++) { %>
          <tbody>
            <tr>
              <td><%= appointments[i].date %></td>
              <td><%= appointments[i].fullName %></td>
              <td><%= appointments[i].phoneNumber %></td>
              <td><%= appointments[i].problemDescription %></td>
            </tr>
          </tbody>
          <% } %>
        </table>
        <% } %>
      </div>
    </section>
  </body>
</html>
